# Deferred items and issues

| Item | Reason | Opened |
|---|---|---|
| Operators GUARDCOLLAPSE, GUARDSPLIT | Need compound-statement end detection (nested `if` with dangling `else`). v1 detects simple statements and conditions only. | 2026-09-07 |
| Mutating `while` conditions | Requires body rewriting (`while true do begin … break`). v1 mutates `if` and `until` conditions only. | 2026-09-07 |
| Mutating `if` conditions in `else if`, `then if`, `do if`, or case-branch position | Requires wrapping the whole `if` statement in `begin…end`, which needs compound-statement end detection. | 2026-09-07 |
| Objects other than codeunits (table/page triggers) | Same tokenizer would work; cut for POC size. | 2026-09-07 |
| Phase 4 triage page, Phase 5 incremental runs, Suggest-Test | After Gate G0. | 2026-09-07 |
| Pester 6.1.0 is what an unpinned Install-Module returns | Spec §9.3 pins Pester 5.x; all test runs import with -MaximumVersion 5.99. Pester 6 compatibility unverified. | 2026-09-07 |
| `node --test dist/test/` (§6.4 toolchain script, T15) fails on the installed Node v22.19.0 (Windows): `node --test <bare-directory-path>` throws `Error: Cannot find module 'C:\...\dist\test' ... code: 'MODULE_NOT_FOUND'` — it tries to `require()` the directory instead of recursively discovering test files in it (confirmed with both a trailing slash and without, on both Bash and native PowerShell, and reproduced in an isolated scratch directory unrelated to this repo). A glob (`node --test dist/test/*.test.js`) and no-path auto-discovery (`node --test`) both work correctly on this same Node install. `generator/package.json`'s `test` script was changed to `npm run build && node --test dist/test/*.test.js` instead of the spec's literal `dist/test/`. | 2026-09-07 |
| Register-ObjectEvent stream capture reorders lines | PowerShell event queue gives no ordering guarantee; Invoke-Continia now uses StandardOutput.ReadToEndAsync/StandardError.ReadToEndAsync with WaitForExit timeout. | 2026-09-07 |
| T06 brief's `Assert: Codeunit Assert` does not compile against the Microsoft "Library Assert" app (28.1.49838.50268, target env `mut-spike-01`) | `error AL0185: Codeunit 'Assert' is missing` on both `MUTFxOrderTests.Codeunit.al(7,17)` and `MUTFxU4SpikeTests.Codeunit.al(7,17)`. The app's own object is `codeunit 130002 "Library Assert"` (namespace `System.TestLibraries.Utilities`), not `Assert`. Fixed by declaring `Assert: Codeunit "Library Assert";` in both test codeunits (variable name `Assert` kept, per the brief's intent; only the type name changed). Method names used (`IsTrue`, `IsFalse`, `AreEqual`, `ExpectedError`) match this codeunit's signatures. Does not touch the normative §6.3.1 bodies. | 2026-09-07 |
| **T06 IndirectInsert — RESOLVED**: fixture test codeunits set `TestPermissions = Disabled` per spec §6.3.2 after spike U4b; permission set (`MUT Fx All`) kept. Confirmed live: 50300 9/9 pass, 0 skipped. | 2026-09-08 |
| **T04 fix round 1 BLOCKED — same symptom, different diagnosis attempted, unresolved**: reproduces the T06 entry above exactly (`test run <envId> 50300 --json` → 9 total, 0 passed, 0 failed, 9 skipped, 0 duration, every test `Skip`) after implementing a controller-issued, live-verified diagnosis that `MUT Test Hooks`' `Setup.GetOrCreate()` call was denied under the restricted DemoPortal test-session user because a codeunit's `Permissions` property only elevates access for code textually inside that codeunit, not code inside a table method it calls. Fix applied verbatim: `Permissions` narrowed to `tabledata "MUT Mutation Setup" = R, tabledata "MUT Mutant Result" = RI`; both event subscribers now call `if not Setup.Get(0) then exit;` directly instead of `Setup.GetOrCreate()`. Compiled clean (0 errors/0 warnings) and redeployed (`published: true`, `readinessOutcome: "ready"`) to `mut-spike-01`. Live `test run` on codeunit 50300 immediately afterward (twice, jobs 16 and 17) still shows 9/9 `Skip` — identical to the pre-fix symptom and to the T06 entry. Either the permission fix is necessary but not sufficient (the T06 execution-pass hypothesis may still be the primary or an additional blocker), or the fix has not taken effect for a reason invisible from the CLI. The code change is in `core-app/src/MUTTestHooks.Codeunit.al`, working tree only, **not committed** (the prescribed commit message asserts a resolution that was not observed). Full JSON test-run evidence in `task-T04-report.md`'s "Fix round 1" section. | 2026-09-08 |
| **T04 fix round 2 RESOLVED** the all-`Skip` symptom logged in the T06 entry and the fix-round-1 entry above. Root cause was that the direct `Setup.Get(0)` call in round 1, though no longer denied by table-level permission indirection, could still raise an uncaught error under the restricted DemoPortal test-session user (missing/undiagnosable from the CLI) and an uncaught error inside `OnBeforeTestMethodRun`/`OnAfterTestMethodRun` causes the platform to skip the test rather than fail it — so any residual error in the hook body, from any cause, produced the identical 9/9-Skip symptom. Fix: hooks now never throw. `MUT Mut` gained `LastHookError: Text` plus `SetLastHookError`/`GetLastHookError` (and `Reset()` clears it); `MUT Test Hooks` wraps the setup read in a new `[TryFunction] local procedure TryReadActiveMutant(var ActiveMutantId: Integer; var RunNo: Integer)` (read-only — no writes, per the TryFunction rule) and both subscribers now branch on its result, recording `'Before '/'After ' + FunctionName + ': ' + GetLastErrorText()` via `MutationCore.SetLastHookError(...)` instead of letting any error propagate; the `MUT Mutant Result` insert in the after-hook is intentionally left unwrapped (an insert failure there is expected to surface as an ordinary test failure, which is acceptable). Confirmed live: `continia test run 30004698-209d-467c-96eb-9b412e9ee6ee 50300 --json` → 9 total, 7 passed, 2 failed, **0 skipped**; the 2 failures are the known, separate fixture-permissions defect (`Sorry, the current permissions prevented the action. (TableData 50200 MUT Fx Order Insert: MUT Fixture Test)`), not a Mutation Core problem. Committed in `core-app/src/MUTMut.Codeunit.al` and `core-app/src/MUTTestHooks.Codeunit.al`. Full JSON test-run evidence in `task-T04-report.md`'s "Fix round 2" section. | 2026-09-08 |
| **T07 field-name correction**: SPEC §6.5.3's `Grant-MutPermissionSet` row and this task's brief describe the Automation API `userPermissions` entity as having a `permissionSetId` field. Verified live against `mut-spike-01` (`GET .../users({uid})/userPermissions`): the actual field is `roleId` (values come back upper-cased, e.g. `"MUT CORE ALL"`, and comparisons use PowerShell's case-insensitive `-eq`); there is no `permissionSetId` field anywhere in the response. `POST` with `{ "roleId": <id>, "appId": <AppId>, "scope": "System" }` succeeds on the first attempt — the brief's fallback path (retry without `scope`, then with `"Tenant"`) was not needed. `Grant-MutPermissionSet` in `orchestrator/backends/DemoPortal.psm1` uses `roleId` for both the read-side match and the POST body; the exported PowerShell parameter is still named `-PermissionSetId` per the spec's function signature. | 2026-09-08 |
| **T07 LIVE RESULT — permission grant did not fix the two `MUT Fx Order` `IndirectInsert` failures the T06/T04-fix-round-2 entries attributed to missing grants**: `Grant-MutPermissionSet` was run live for both `MUT Core All` and `MUT Fx All` against every user the Automation API's `GET companies({cid})/users` returns for `mut-spike-01` — four users (`RB`, `ADMIN`, `EH`, `RF`), one more than the three DemoPortal itself lists via `continia env users <id> --json` (`Rf`/`Super User`, `EH`/`Controller`, `RB`/`Approver`; `ADMIN` is a fourth BC user not in that list). All four already held `SUPER` (`scope: System`, confirmed via `GET userPermissions` before granting) — which alone should make every table operation succeed regardless of any other permission set — and after granting, all four hold `SUPER`, `MUT CORE ALL`, and `MUT FX ALL`. Immediately afterward, `continia test run 30004698-209d-467c-96eb-9b412e9ee6ee 50300 --json` still returned 9 total, **7 passed, 2 failed, 0 skipped** — byte-for-byte the same failure the T04 fix-round-2 entry recorded before any grant existed: `PostOrder_PositiveQty_SetsPosted` and `PostOrder_ZeroQty_Errors` both fail with `Sorry, the current permissions prevented the action. (TableData 50200 MUT Fx Order IndirectInsert: MUT Fixture Test)` on the test codeunit's own `FxOrder.Insert(true)` call (the test codeunit itself declares `Permissions = tabledata "MUT Fx Order" = RIMD`, per §9.2). Because every named environment user already had `SUPER` and the failure persisted unchanged, **the restricted "DemoPortal test session" identity described in §6.1.4 cannot be any of `mut-spike-01`'s own named BC users** — granting permission sets to them, which is the entire mechanism §6.1.5b and the T06 "RESOLVED by design" note rely on, has no effect on this specific failure. `docs/spike-baseline.md` §U8 records the 7/9 outcome per this task's own acceptance criteria (DONE_WITH_CONCERNS, not a loop); the actual identity/mechanism running headless `test run` jobs, and why it is immune to grants made to every enumerable user, is unresolved and needs a follow-up spike before Tier B/fixture runs can rely on Grant-MutPermissionSet to unblock table-writing tests. | 2026-09-08 |
| **T05 HookErrorIsEmpty result on mut-spike-01: `Assert.AreEqual failed. Expected:<> (Text). Actual:<Before HookErrorIsEmpty: Sorry, the current permissions prevented the action. (TableData 50000 MUT Mutation Setup IndirectRead: Mutation Core)> (Text). MUT Test Hooks swallowed an error.`** — confirms the suspicion in this task's context: `MUT Test Hooks`' `TryReadActiveMutant` (a `[TryFunction]` doing `Setup.Get(0)` directly inside the hook codeunit, per §6.1.4/§6.1.5b) still cannot read table `MUT Mutation Setup` inside a DemoPortal test session, even though the hook no longer throws (T04 fix round 2) and the codeunit declares `Permissions = tabledata "MUT Mutation Setup" = R, tabledata "MUT Mutant Result" = RI`. The other three unit tests (`SetActive_ThenActiveMatchesOnlyThatId`, `Reset_ClearsActive`, `Active_ZeroWhenNothingSet`) pass (3/4 total, 1 failed as expected). This is the same restricted-test-session mechanism the T07 entry above found immune to granting permission sets to every enumerable environment user; a follow-up spike is needed to identify the actual identity running headless `test run` jobs before the hooks can reliably read setup rows. Full stack trace in `task-T05-report.md`. | 2026-09-08 |
| **T04b (U4b spike) RESOLVED the table-read restriction with an IsolatedStorage channel — DemoPortal backend viable**: Experiment A (control) confirmed the restriction is TestPermissions-mode-specific: adding `TestPermissions = Disabled;` to codeunit 50400 made `Setup.Get(0)` inside the hooks succeed (`test run 50400` → 4/4 pass, `HookErrorIsEmpty` passes); reverted afterward (not shipped — shipping `Disabled` would defeat the point of testing under restricted permissions). Experiment B replaced the table read with IsolatedStorage (`DataScope::Module`), which is not subject to table permissions: table 50000 "MUT Mutation Setup" gained `OnInsert`/`OnModify` triggers calling a new `MirrorToIsolatedStorage()` that writes `'ActiveMutantId'`/`'CurrentRunNo'` via `IsolatedStorage.Set(Key, Format(Field, 0, 9), DataScope::Module)`; `MUT Test Hooks`' `TryReadActiveMutant` now does `IsolatedStorage.Get(Key, DataScope::Module, Value)` + `Evaluate(Field, Value, 9)` (defaulting to 0 on a missing key) instead of `Setup.Get(0)`. Under the DEFAULT (restrictive) mode, after one API PATCH to `mutationSetup(0)` primed the mirror: `test run 50400` → 4/4 pass, `HookErrorIsEmpty` passes. End-to-end proof: PATCHed `activeMutantId=999, currentRunNo=1`; `test run 50301` (one passing, one deliberately-failing fixture test) → 1 passed, 1 failed as expected; GET `mutantResults` returned exactly one row `{runNo:1, mutantId:999, status:"Killed", killingTest:"MUT Fx U4 Spike Tests:U4_Failing"}` — the after-hook's own `Insert()` on `MUT Mutant Result` succeeded (unlike the brief's expectation that it would probably be denied too), so **the orchestrator's §5-step-5 POST fallback is not needed for this path**, though it remains the documented safety net. Setup PATCHed back to `activeMutantId=0, currentRunNo=0` and confirmed via GET. Committed in `core-app/src/MUTMutationSetup.Table.al` and `core-app/src/MUTTestHooks.Codeunit.al`; `Permissions` on the hooks codeunit left unchanged (now unused by the read path but still declared, harmless). Full JSON evidence in `task-T04b-report.md`. | 2026-09-08 |
| **T16 §6.4.3 "compound keyword" list includes `with`, which the §6.4.1 KEYWORDS set (T15) does not tokenize as `keyword`**: `with` never appears in `Token.kind === 'keyword'` (it is not valid modern AL syntax; the tokenizer emits it, if it ever appears, as a plain `identifier`). Resolved as a no-op: `findSimpleStatements`'s "not a compound keyword" exclusion only needs to check for `if`/`while`/`repeat`/`case`/`for`/`foreach`/`begin` (all present in KEYWORDS) — `with` can never reach that check as a keyword token, so omitting it from the exclusion set changes no behavior. No code change needed; noted so a future reader does not "fix" a mismatch that has no effect. | 2026-09-08 |
| **T16 `findObjectHeader` field derivation (not fully spelled out by §6.4.2's interface comment)**: `objectType` is the first significant token's raw text (not lower-cased — the fixture's own source is already lowercase `codeunit`; case-folding for the exclusion check in §6.4.6 is left to that consumer). `objectId` is `Number.parseInt` of the second significant token's `number` text. `objectName` strips the surrounding `"…"` from a `quotedIdentifier` token's text (the tokenizer keeps the quotes in `Token.text`, per the T15 golden fixtures) or is used verbatim for a bare `identifier` object name. Comments/preprocessor tokens before or between the three header tokens are skipped when locating them (consistent with the brief's "skip them when looking for the previous significant token" guidance, extended here to header parsing for robustness). | 2026-09-08 |
| **T12 SPEC §1.1's Tier B table is stale for AUT codeunit 72918690 / 72918691 and test codeunit 95913 — confirmed against the live read-only checkout, `git log` shows no history of the missing objects on this branch (HEAD `cc8f0168` at the time of this check, 2026-09-08)**: `C:\GeneralDev\AL\Continia Banking Master\Continia Banking\base-application\Bank\Codeunits\LineDateImportDef.Codeunit.al` declares `codeunit 72918690 "CTS-CB Line Date Import Def."` — not `"CTS-CB Auth Granted Acc. Mgt"` as SPEC §1.1 states, and there is no file named `AuthGrantedAccMgt.Codeunit.al` anywhere in `base-application`. Object id 72918691 is not used by any codeunit in `base-application` at all (`BankAccExternalID.Codeunit.al` does not exist). In `base-application-test`, `TestAuthGrantedAcc.Codeunit.al` does not exist and codeunit id 95913 is unused (`grep -rn "^codeunit 959" base-application-test` tops out at 95912 "CTS-CB Setup Data Import Tests"); a live `continia.exe test run <mut-spike-01> 95913 --json` against the deployed AUT copy confirms this at runtime: it returns `{"status":"completed","passed":true,"summary":{"total":0,"passed":0,"failed":0,"skipped":0,"durationSeconds":0,"codeunitName":""},"tests":[]}` — the CLI does not error on an unknown codeunit id, it silently reports zero tests. `DemoPortal environment list` shows two environments named `AuthGrantedAccountApply` and `build-2026-09-03-auth-granted-accounts-share-design` (not `mut-*`, not touched), suggesting the Auth Granted Account feature exists on an unmerged branch/environment, not in this checkout. Codeunit 95155 "CTS-CB Test Auth Share Detect" and AUT codeunit 72918635 "CTS-CB Auth Share Detection" DO match SPEC exactly. Impact on T12: the single-method U7 job and the coverage sample were both taken from codeunit 95155 instead (documented in `spikes/tier-b/Invoke-TierBBaseline.ps1`'s header comment); codeunit 95913's row in the Tier B baseline table records the observed 0/0/0 rather than the expected 15/15. This needs resolving (repoint SPEC §1.1 at real object ids, or wait for the feature branch to merge) before any task that depends on a real, passing codeunit 95913 baseline. | 2026-09-08 |
| **T24: SPEC §6.5.4 step 7 / §6.5.5 name the covering-test-selection function `Get-CoveringTests`, but the task instructions (and the brief's own Interfaces section) specify `Get-MutCoveringTests`** | Implemented as `Get-MutCoveringTests` (in `orchestrator/lib/Coverage.psm1`), matching the `Mut`-prefix convention used by every other exported orchestrator function (`Get-MutCoverage`, `Get-MutReferenceMap`, `Invoke-MutTests`, …) and the explicit task instructions. A future task consuming this function (e.g. the `Get-CoveringTests` orchestration step, `MutantLoop.psm1`) must call it as `Get-MutCoveringTests`, not the unprefixed name SPEC §6.5.4/§6.5.5 prose uses. | 2026-09-08 |
| **T12 U9: `test coverage --json`'s `csv` field has no header row, and 5 columns rather than the 4 SPEC §6.5.5/§7.2 assume**: live against job 41 (`test coverage 30004698-209d-467c-96eb-9b412e9ee6ee 41 --json`), every line of the CSV — including the first — is a data row, e.g. `"Codeunit","50000","Object","0","0"`. Columns are positional: `ObjectType` (only value observed: `"Codeunit"`), `ObjectId` (observed: `130000`, `2000000002`, `50000`, `50001`, `72918635`, `95001`, `95155` — Test Runner Mgt, a virtual/system id, Mutation Core's own codeunits, the AUT codeunit under test, and the test codeunit itself all appear in one job's coverage, not just the target object), a line-classification string (`"Object"`/`"Trigger/Function"`/`"Empty"`/`"Code"`, not documented anywhere in SPEC), `LineNo`, `Hits`. There is no `jobId` or `id` property on `test run --json`'s top-level response either (F18 confirmed: properties are exactly `status, passed, summary, tests`); the job number is exposed only in the CLI's human-mode (non-`--json`) text output as `Test job started: N`, and `test coverage <envId> <N> --json` accepts that plain integer and returns `{envId, jobId, csv}` correctly. §6.5.5's planned `ConvertFrom-MutCoverageCsv -Csv` ("MUST use header names, not positions, and MUST throw on an unknown header set") cannot be implemented as specified against this data — there are no header names to read. `fixtures/coverage/sample.csv` (T12) is the first 200 raw lines of job 41's CSV verbatim (all codeunit 50000 rows, since that's numerically first); the AUT's own codeunit 72918635 rows (487 of them) start later in the 1691-line document and are not in the saved sample. Whoever implements `Get-MutReferenceMap`/`Coverage.psm1` (T24+) needs either a positional parser with the 5-column shape above, or a follow-up spike to see whether a header row exists on some other job shape. | 2026-09-08 |
| **T27 live fixture run 1 (first attempt): mutant 1 (`IsLargeOrder` REL, expected `Survived`) came back `Timeout` under `mutation.fixture.config.json`'s illustrative `timeouts: { perTestFactor: 5, minSeconds: 60, jobOverheadSeconds: 0 }` (copied verbatim from SPEC §6.5.1's example config; `docs/spike-baseline.md`'s Recommendation row for `timeouts.jobOverheadSeconds` was never filled in by an earlier spike)**: with the fixture's single covering codeunit (50300, ~9 fast tests, baseline `durationsByCodeunit` on the order of tens of ms), `Get-MutTimeoutBudget` computes `max(60, ceil(5 × ~0.07s)) = 60s` for essentially every mutant — the 60s floor, not the scaling term, dominates. Live against `mut-spike-01`, the very first mutant's `Invoke-MutTestsWithBudget` job exceeded that 60s floor (confirmed via `Reset-MutEnvironment` firing and the environment observed in `Starting` status immediately afterward, and `results.jsonl`'s first line reading `{"Id":1,"Status":"Timeout",...}`), even though this mutant is not one of the three genuinely-infinite-looping mutants §6.3.3 expects to time out. This is consistent with the round trip immediately following `Publish-MutSchemata`'s own full-suite rerun (a large compile+publish+full-suite-test sequence) rather than a systemic per-job latency problem — the same environment's historical job times recorded elsewhere in this file (U7: ~9-11s for a 13-test codeunit) are well under 60s. Root-caused as an under-tuned `timeouts.minSeconds` for the fixture's very-fast baseline (a floor sized for "fast tests, but zero margin for network/scheduling/environment-settling overhead"), not a defect in `Get-MutTimeoutBudget`, `Invoke-MutMutantLoop`, or any other module — `MutantLoop.Tests.ps1`'s existing unit tests already cover the budget formula correctly per §6.5.6. Fix applied (this task, before re-running): bumped `mutation.fixture.config.json`'s `timeouts.minSeconds` from 60 to 120 (doubling the floor's margin; `perTestFactor`/`jobOverheadSeconds` left unchanged since they were not implicated — the floor, not the scaling term, was the binding constraint). The flawed first attempt's run-1 artifacts (`out/runs/1/*`) were discarded and the fixture run repeated from scratch under the corrected config; see this task's report for the clean run's outcome. A future task tuning Tier B's real `mutation.config.json` should not assume `minSeconds: 60` is sufficient headroom purely from a fast baseline-duration computation. | 2026-09-08 |
| **T27 live fixture run 1 (second attempt), genuine defect in `MutantLoop.psm1`'s `Invoke-MutTestsWithBudget`: `Start-Job` hangs indefinitely (confirmed past 400+ seconds of wall clock, not merely slow) the moment its own separate-process PowerShell instance tries to spawn the backend's CLI tool as a further child process via `System.Diagnostics.Process`, even though the exact same call made directly from an interactive-context process completes normally (~13-37s observed live, repeatedly)** — reproduced in isolation outside the orchestrator (direct `Start-Job` + `Wait-Job` + `Receive-Job` against `mut-spike-01`, bypassing every other module) to rule out an environment- or config-tuning cause before concluding this. With every mutant activation hitting this, the mutant loop could never complete a single mutant against the live backend. Root-caused as `Start-Job`'s own OS-process isolation interacting badly with a further nested child-process spawn on this machine (exact OS-level mechanism not confirmed — window-station/desktop inheritance and non-interactive-context network/proxy resolution are both plausible, per public reports of similar `Start-Job` + nested-process issues, but this was not narrowed further once a working alternative was confirmed). Fix: `Invoke-MutTestsWithBudget` now runs the same call on a background **runspace** (`[runspacefactory]::CreateRunspace()` + `[System.Management.Automation.PowerShell]`) hosted as a thread of the SAME process instead of a `Start-Job` background job — no additional OS-process boundary for the backend's CLI spawn to cross. Confirmed live: the identical mutant-1 scenario that hung indefinitely under `Start-Job` completed in ~13.6s under the runspace version, with the correct `Survived` result. Public contract (`{TimedOut; Result; ErrorMessage}`) and every existing `MutantLoop.Tests.ps1` assertion are unchanged; the wall-clock-kill unit test (`Invoke-MutTestsWithBudget (real background-runspace wall-clock kill)`) still passes using `.Stop()` in place of `Stop-Job`. `Invoke-MutMutantLoop` and the rest of the module's control flow are untouched. | 2026-09-09 |
| **T27 live fixture run 1 (second/third attempts): the test run immediately following `Reset-MutEnvironment` (§6.5.6 step 3, on a genuine mutant timeout) twice came back as a clean completion (no timeout, no error) reporting ZERO tests actually executed (`Passed = 0, Failed = 0`) instead of running the covering codeunit's real suite — silently recorded as a false `Survived` for the very next mutant in id order (observed for mutant ids 22 and 26, both immediately following a genuinely-infinite-looping mutant's timeout+reset).** A first fix attempt (a fixed 45s `Start-MutPostResetSettle` delay after every `Reset-MutEnvironment`, on the theory that the backend's "Running" status does not guarantee the test-execution service is truly ready) was applied and unit-tested, but the identical empty-result symptom recurred live even with that delay in place, disproving "not long enough yet" as the primary explanation on its own. Rather than continue guessing at a longer fixed duration with no evidence for how long is actually enough, `Invoke-MutMutantLoop` now retries the SAME test invocation once, unconditionally, whenever a completed (non-timeout, non-error) result reports zero total tests (`Passed + Failed == 0`) — directly targeting the observed symptom instead of a specific wait duration. The `Start-MutPostResetSettle` delay is left in place alongside it (cheap, and plausibly still reduces how often the retry path is needed) rather than removed, since removing an unconfirmed-harmless mitigation was not the priority once a targeted, symptom-based fix existed. Unresolved: the true root cause of the empty-result response itself (a BC-side warm-up/cache effect after a service restart is the leading hypothesis, unconfirmed) — flagged here for a follow-up spike rather than investigated further under this task's time budget. | 2026-09-09 |
| **T27 own bug (not an earlier task's module): `Run.psm1`'s `Build-MutSchemataStep` corrupted its own `schemata.json` cache — `ConvertFrom-Json` on a JSON array attaches hidden ETS members to the returned `System.Object[]` that make a LATER `ConvertTo-Json` of it, once nested as a property of another object being converted, serialize as `{"value":[...],"Count":N}` instead of a plain JSON array** (reproduced directly and minimally: `[pscustomobject]@{ x = (ConvertFrom-Json '[{"a":1}]') } \| ConvertTo-Json` alone exhibits this — no summary/wrapper object of this module's own construction is needed to trigger it). `Build-MutSchemata`'s (Schemata.psm1) own `.Mutants` property is exactly such an array (from `Get-Content mutants.json | ConvertFrom-Json`, deliberately not re-wrapped in `@()` for the reason documented in that module already). Storing it directly as `schemata.json`'s `mutants` field wrote the corrupted shape; reading it back on a resumed run (Build-MutSchemataStep's own marker-skip path) then collapsed all 26 mutants into a single malformed object with no `objectId`/`procedure`/etc. properties, crashing `Get-MutCoveringTests` downstream (`PropertyNotFoundException`) the first time a run was resumed past the schemata-build step. Fixed by rebuilding the array via `@($result.Mutants | ForEach-Object { $_ })` before serializing (and, defensively, on the read-back path too) — piping through `ForEach-Object` produces a plain `System.Object[]` with none of `ConvertFrom-Json`'s extra members. Regression test added: `Run.Tests.ps1`'s `Build-MutSchemataStep caches mutants as a plain JSON array...` test constructs its mock's `.Mutants` via a real `ConvertFrom-Json` call specifically so it reproduces the bug if the fix regresses (a literal `@(...)` array never reproduces it). Any other cached JSON artifact in `Run.psm1` that stores a whole array sourced directly from `ConvertFrom-Json` (rather than one built via a `+=`/`ForEach-Object` loop of freshly-constructed objects) is exposed to the same risk; audited at fix time and none of `Run.psm1`'s other cache writes (baseline.json, coverage.json, covering.json, loop-results.json) do this. | 2026-09-09 |
| **T27 live fixture run 1 (fourth attempt): resuming the mutant loop for an already-recorded `RunNo` re-POSTs a `Survived` result for a mutant that already has one from an earlier attempt, and the earlier task's `MutantLoop.psm1` had no idempotency guard on that path** (unlike the `Killed` branch, which already GETs first and skips the POST if a row exists) — the API rejects the duplicate `(runNo, mutantId)` key (`Internal_EntityWithSameKeyExists`), and that error was previously unhandled, ending the whole run. This is a real, live-reachable case, not a hypothetical: a run resumed at (or re-run past) the mutant-loop step for the same `RunNo` — exactly what several of this task's own retries against `mut-spike-01` needed to do after upstream fixes — will always hit it for any mutant that was Survived on a prior attempt. Fixed by wrapping the Survived POST in a try/catch that swallows only an `EntityWithSameKeyExists` conflict (checked via `$_.ErrorDetails.Message` first, falling back to `$_.Exception.Message`) as an idempotent no-op, matching the Killed branch's own idempotency; any other POST failure still propagates and ends the run as before. Two regression tests added to `MutantLoop.Tests.ps1` covering both the swallowed-conflict path and the still-throws-on-any-other-error path. | 2026-09-09 |
| **T27 own bug (not an earlier task's module): `Run.psm1`'s `Ensure-MutEnvironment` marker-skip path returned a cached environment handle without calling any backend function at all, so a run resumed at a step later than "ensure environment" never primed the backend module's own module-scoped CLI-path state that several of its functions (e.g. `Invoke-MutApi`'s `Get-MutApiBase`/`Get-MutCredential` chain) depend on some EARLIER call in the same process having already set** — the very first `Invoke-MutApi` call of such a resumed run crashed immediately (`The variable '$script:CliPath' cannot be retrieved because it has not been set`). Fixed: `Ensure-MutEnvironment` now always calls `Get-MutEnvironment` (and `Start-MutEnvironment` when not already `Running`) even when its own marker+cache exist, and only skips the expensive `Sync-MutAutCopy` call in that case — cheap re-verification of environment health on every call, not just a priming side effect, and arguably a better design for a resumed run than blindly trusting a cache written by a possibly much earlier attempt. `Run.Tests.ps1`'s marker-skip test updated to expect this (only `Get-MutEnvironment`, nothing else, on a second call). | 2026-09-09 |
| **T27 BREAK-acceptance run, genuine defect in `generator/src/generate.ts` (an earlier task's module, T18-ish): a BREAK candidate was gated by BOTH `options.includeBreak` AND membership in the `--operators` allow-list, but §6.4.5's own catalog table documents BREAK as "Only with `--include-break`" -- gated by that flag ALONE, independent of `--operators` (which selects among the six normal operators: REL/BOOL/NOT/COND/DEL/INSFLAG).** `mutation.fixture.config.json`'s `generator.operators` list (and this task's brief, which instructs flipping only `generator.includeBreak` to make the BREAK-acceptance config) never lists `"BREAK"` — so the live BREAK-acceptance run generated a schemata with **zero** BREAK mutants (confirmed: `generated 26 mutants, skipped 2`, identical to the non-BREAK count) and would have completed as an unremarkable 26-mutant run, silently failing to exercise §8 acceptance item 2 at all. Caught before committing a false-positive result by inspecting the generated `mutants.json` directly once the live pipeline had already built the (wrong) schemata; that pipeline attempt was killed and its `out/runs/2/*` discarded. Fix (generator/src/generate.ts, statement-operator loop only -- the condition-operator loop already excludes BREAK by `operator.kind !== 'condition'` before ever reaching the allow-list check, so needed no change): BREAK's eligibility now checks only `options.includeBreak`, bypassing the `--operators` allow-list entirely, while every other statement operator is still gated by it as before. Two regression tests added to `generator/test/golden.test.ts` (one reproducing this exact config shape and asserting BREAK mutants are produced and the 26 non-BREAK mutants are unaffected; one confirming `includeBreak: false` still yields zero BREAK mutants regardless of the operators list) — 105/105 generator tests pass. Verified locally afterward: the fixture AUT with this config now yields 41 mutants (26 normal + 15 BREAK, one per simple statement), before repeating the live BREAK-acceptance run. | 2026-09-09 |
| **T27 own bug (not an earlier task's module), found while re-checking the BREAK-acceptance path after the generator fix above: `Run.psm1` never gave `Export-MutResultsStep` any row to report for a compile-error mutant at all.** A compile-error id (`$schemata.CompileErrorIds`, e.g. every BREAK mutant) is, by construction, excluded from every generator iteration after the one that first hit it (§6.5.4 step 4) -- so it never appears in `$schemata.Mutants`, the list `Export-MutResultsStep` was passed. `Get-MutMergedMutantRows` (Results.psm1) already correctly marks any id present in `CompileErrorIds` as `CompileError` "regardless of whether it also has a Results row" -- but it only ever produces a row for something it iterates over in the first place, so a compile-error mutant would have had NO row at all in `results/<RunNo>.json`, failing §8 acceptance item 2 ("all BREAK mutants end as CompileError") outright rather than merely mis-reporting them. Fixed in two places: `Schemata.psm1`'s `Build-MutSchemata` now also accumulates and returns `ExcludedMutants` (the full §7.1-shaped record for every excluded id, captured at the same point it already captured each one's stableKey, before that iteration's generation output is overwritten by the next); `Run.psm1`'s `Build-MutSchemataStep` caches/reconstructs that list the same (ForEach-Object-flattened) way as `Mutants` itself, and `Invoke-MutRunPipeline` now passes the UNION of `$schemata.Mutants` and `$schemata.ExcludedMutants` to `Export-MutResultsStep`, instead of `$schemata.Mutants` alone. Regression tests added: `Schemata.Tests.ps1`'s existing compile-error-exclusion test extended to assert `ExcludedMutants` carries the excluded ids' full records; `Run.Tests.ps1`'s mocked `Build-MutSchemata` return values updated to include the new field (Set-StrictMode would otherwise throw `PropertyNotFoundException` reading it, which is exactly how this was first caught turning green). | 2026-09-09 |
| **T13 BLOCKED: the AUT source has moved to BC platform/application 29.0.0.0 (`out/aut-original/app.json`: `"version": "29.0.0.0"`, `"platform": "29.0.0.0"`, `"application": "29.0.0.0"`, all 5 Continia dependencies pinned to `29.0.0.0`), but `mut-spike-01` is a BC 28.1 sandbox (`env get`: `bcVersion: "28.1.0.0"`, `platformVersion/applicationVersion: "28.1.49838.50268"`) — the AUT copy cannot be compiled/published on this environment at all, for any mutant.** First symptom: `Publish-MutApp` on a fresh `Sync-MutAutCopy` copy returned `Success=False` in 3.3s with `Get-MutFirstDiagnostic` reporting "no diagnostics returned" (a `Publish-MutApp` follow-up defect noted below). The raw CLI JSON (`deploy ... --json`) actually carries `"code": "symbol-fetch-failed"` and a detailed `error` string: every one of the AUT's 7 symbol dependencies (5 Continia apps + `Microsoft_Application`/`Microsoft_System`) fails with `fetch-failed: dev-endpoint package failed validation (<currently-installed version>)`, including `Microsoft_Application`/`Microsoft_System` whose parenthetical versions (`28.1.49838.50268`/`28.0.50197.0`) exactly match what is installed — ruling out a simple version-mismatch-of-one-dependency explanation and pointing instead at the platform/application version gate itself. Diagnosis steps taken (all against `mut-spike-01`, the only environment touched): (1) reproduced 3× with the direct CLI, identical every time; (2) `env apps --all --json` confirmed the 5 Continia deps are at `28.x`, not `29.0.0.0`; (3) `deps install <envId> out/aut-original --json` opportunistically upgraded 3 of the 5 (Continia Core/System Application/Connector App to `30.0.0.0` — a version the DemoPortal catalogue happened to have), leaving Approval and Banking - Permission Sets at `28.5.0.0` (`"reason": "not found in catalogue"` for both at any `>= 29.0.0.0` build); retrying the deploy afterward failed identically, now citing `30.0.0.0` in the parens for those three; (4) deleted the stale `out/aut-original/.alpackages/*.app` (dated 2026-09-08/09, never refreshed by `Sync-MutAutCopy` since its robocopy mirror excludes `.alpackages` via `/XD`) and retried — identical failure, ruling out a stale local symbol cache; (5) `continia compile core-app --env <id> --json` on an unrelated, small, already-BC-28.1-targeted app succeeded cleanly and even live-refreshed a `Microsoft_System` symbol via the dev endpoint, proving the environment's dev-endpoint symbol service is healthy in general, not globally degraded; (6) `env profiles versions` confirms a `29.0.0.0` profile exists in the catalogue, but creating a new environment from it is out of scope (guardrail: `mut-spike-01` is the only environment this task may touch) and `continia env` has no in-place upgrade command. Conclusion: this is a genuine, structural BC-major-version gate, not a symbol-cache, deps-install, or auth issue, and it blocks 100% of the 20 hand mutants (all live in the one AUT codeunit) until either `mut-spike-01` is replaced/upgraded to a BC 29.0 environment or the AUT source rolls back below 29.0.0.0 (neither of which this task may do). Zero mutants were applied to the copy; the environment's installed Continia dependency versions were left as the diagnosis left them (3 apps at `30.0.0.0` instead of the original `28.x`/`28.6.0.0`) since reverting them would not change the outcome and no read-only guardrail covers `mut-spike-01`'s installed-app state. Follow-up defect (not fixed under this task, per instruction): `Publish-MutApp` (`orchestrator/backends/DemoPortal.psm1`) already parses `row.code` into its `Code` output field, but does not surface `row.error` (the CLI's own human-readable message) anywhere in its return object; a caller that only inspects `Diagnostics` (empty here, since this failure shape has no `diagnostics` array) sees nothing useful. `Code` should be logged/reported by callers, and `Publish-MutApp` arguably should also read `row.error` into e.g. an `ErrorMessage` field for this failure shape. | 2026-09-17 |
| **T27 own bug (not an earlier task's module): `Run.psm1`'s `Push-MutManifest` only checked for an existing mutant by `id`, but the `MUT Mutant` table's own unique index is on `Stable Key`, not (only) `Id`.** Mutant ids are positional within one generator `generate()` call (§6.4.8) and shift entirely when the generator's flags change (e.g. adding BREAK candidates re-numbers essentially everything after the first BREAK-eligible statement), even though many individual mutations are semantically unchanged between two runs with different flags and so keep the SAME `stableKey` (deterministic from mutation content: object/procedure/operator/normalized text/occurrence -- not from id). Live evidence, hit while re-running the BREAK-acceptance run (RunNo 2) after the generator fix above, against an environment that already had RunNo 1's (non-BREAK) manifest posted: POSTing RunNo 2's differently-numbered mutants hit the table's unique index on Stable Key (`Internal_EntityWithSameKeyExists`) the first time a RunNo-2 id's stableKey matched an already-posted RunNo-1 id's stableKey -- an unhandled, run-ending error, exactly like the earlier `Survived`-POST duplicate-key issue but on the manifest table instead of the results table. Fixed: `Push-MutManifest` now also collects every existing `stableKey` from the same `GET mutants` call it already made, and skips POSTing a mutant whose id OR whose stableKey already exists -- matching the table's actual uniqueness constraint rather than only its primary key. Regression test added to `Run.Tests.ps1` reproducing exactly this shape (an existing row under a different id sharing the new mutant's stableKey) and asserting the POST is skipped while an unrelated, genuinely-new stableKey still gets POSTed normally. | 2026-09-09 |
| **T13b: creating a genuinely fresh BC 29 environment (`mut-spike-02`) exposed a chicken-and-egg gap in `Wait-MutEnvironmentSettled`'s test-readiness probe, and re-exposed the exact `@(pipe \| ConvertFrom-Json)`-nesting bug T11 already found and fixed once, this time in a different script.** (1) `mutation.config.json`'s `demoPortal.settleProbe` points at codeunit 95155/`UpdatePlaceholderRows_EmptyInputs_BecomesNoMatchingAccounts` -- the AUT's *own* test codeunit. On `mut-spike-01` (long-lived, already provisioned from earlier tasks) that codeunit already existed, so the probe always worked; on a brand-new `mut-spike-02`, before the AUT+test app are ever deployed, `test run <id> 95155 <function> --json --timeout 120` returns `summary.total:0` (codeunit not found, not an unready environment) every single time, so `New-MutEnvironment`/`Start-MutEnvironment` threw after all 10 probe attempts (~10.3 min wall clock) without ever reaching the activation-app install or `env use` steps that come after the probe in `Start-MutEnvironment`'s own code. Worked around (not a code fix, since the probe's underlying assumption -- "the settle-probe codeunit already exists" -- genuinely does not hold for a first-ever deploy, and changing the shared settle-probe contract was out of this task's scope): ran the CLI's `deps install-by-id <id> c3755ece-dab0-4d16-987d-040661f18522 --json` and `env use <id>` directly (the exact two calls `Start-MutEnvironment` would have made), then proceeded with `Sync-MutAutCopy` → `Install-MutDependencies` → `Publish-MutApp` (core-app, aut-original, test-app) → `Grant-MutPermissionSet` → baseline (13/13 pass) as normal; once 95155 existed, later `Get-MutEnvironment` calls saw `Status:Running` directly from `env get` and never needed to re-run the probe. Environments that get created from scratch in future tasks will hit this every time until either the settle probe is changed to target a codeunit that pre-exists on every fresh environment (e.g. a Microsoft or Mutation-Core one, not the AUT's own), or `Start-MutEnvironment` is reordered to install the activation app / `env use` before the probe. (2) Separately, `spikes/hand-mutants/Invoke-HandMutants.ps1`'s mutant loader used `$mutants = @(Get-Content -Path $mutantsPath -Raw \| ConvertFrom-Json)` -- the identical anti-pattern T11's `docs/spike-baseline.md` U5 row already documented and fixed in `spikes/u5-u6/Invoke-U5Spike.ps1`: wrapping `@()` directly around a `\| ConvertFrom-Json` pipeline, on this host's Windows PowerShell 5.1 (build 5.1.26100.9444), collapses a 20-element JSON array into a single element whose properties are themselves 20-valued arrays (reproduced deterministically both via `powershell.exe -File` and via a raw `Start-Process` launch, 3/3 repeat runs each; a *bare* `$x = ConvertFrom-Json ...` assignment followed by wrapping the already-materialized variable in `@()` in a SEPARATE statement was reproduced as reliably correct, 3/3 runs). This silently produced `Loaded 1 mutants` and a single combined `CompileError` row (id `"HM01 HM02 … HM20"`) instead of 20 separate mutant rows on the first run attempt. Fixed by splitting the assignment into two statements (parse-mutant-then-wrap) with a comment pointing at this issue; verified fixed by an isolated 3-run repeat before re-running the full harness. Neither `orchestrator/backends/DemoPortal.psm1` (which only ever uses the safe bare `return $stdout \| ConvertFrom-Json` form) nor any other module has this pattern -- confirmed by `Select-String -Pattern '@\(.*ConvertFrom-Json\)'` across `orchestrator/`. Worth a lint/review step for any future spike script that parses JSON: never wrap `@()` directly around a `ConvertFrom-Json` pipeline in the same statement. | 2026-09-17 |
| **T28: first real Tier B run against the actual Continia Banking source tree crashed `generator/src/tokenizer.ts` before a single mutant could be generated, and the crash aborts the ENTIRE `generate` call rather than naming or skipping just the offending file.** `Invoke-MutationRun.ps1 -ConfigPath mutation.config.json -RunNo 3` got past `initialize`/`environment`/`baseline` (13/13 pass) and `references`/`coverage` (both produced), then the `generate` step failed outright: `Invoke-MutGenerator: node ... generate ... --only-objects 72918635 ... exited with code 1. stderr: generate failed: Unexpected character "|" at line 7`. `generate()` (generator/src/generate.ts:235-246) tokenizes **every** `.al` file under `--aut` before `--only-objects` is applied at all (§6.4.8's "ids 1..N over the full enumeration, before any filter"), so one unparseable file anywhere in the 1,072-file real tree aborts the whole run regardless of which object was targeted. Running the built tokenizer directly over every file in `out/aut-original` (a copy of the real AUT) found 20 files it cannot tokenize, none of which exist in the small fixture app used by every previous run: 12 fail with `Unexpected character "|"` — the AL filter-OR operator inside a page's `SourceTableView`/`filter(...)` property, e.g. `out/aut-original/External Codes/Pages/CustLEExtCodeList.Page.al:7`: `SourceTableView = sorting(...) where("External Code Type" = filter("Regulatory Reporting" \| "Local Instrument" \| "Category Purpose" \| "Transaction Purpose"));` — and 8 fail with `Unexpected character "?"` — the AL ternary conditional operator, e.g. `out/aut-original/Templates/Codeunits/TemplateValues.Codeunit.al:322`: `OutputText := ApplyLength(TrimmedText.StartsWith('-') ? TrimmedText.Substring(2) : '-' + TrimmedText, TemplateFieldProp);`. Neither `\|` nor `?` is in `tokenizer.ts`'s `ONE_CHAR_OPERATORS`/`TWO_CHAR_OPERATORS`/`PUNCT` sets, so the tokenizer's final `else` branch throws `Unexpected character ${ch} at line ${startLine}` for both. A latent second defect compounds this: `exclusions.ts`'s `isExcludedFile` already wraps its own internal `tokenize(source)` call in a try/catch, but on failure returns `null` ("not excluded" — proceed as an ordinary codeunit) instead of an exclusion reason, so the failure is silently swallowed once and then reproduced, unguarded, by `generate.ts`'s own direct `tokenize(source)` call a few lines later — which is what actually crashes the process and terminates the whole run without ever naming which of the 1,072 files was at fault (found here only by re-running the built tokenizer standalone against every file). Not fixed under this task (the controller ruled it out of scope — a generator defect for another task to fix); the environment (`mut-spike-02`) was left exactly as found: AUT/test app/Mutation Core still installed, `activeMutantId`/`currentRunNo` both 0 (confirmed via `Invoke-MutApi GET mutationSetup(0)`), no redeploy, no pipeline or CLI process left running, `out/runs/3/{initialize,environment,baseline}.done` plus `baseline.json`/`coverage.json`/`references.json` left in place for a resumed attempt once the generator is fixed. | 2026-09-17 |
| **T21b RESOLVED the two generator defects the T28 row above found: the tokenizer's missing `\|`/`?` operators, and `generate()` aborting the whole run on one unparseable file.** Fixed in `generator/src/tokenizer.ts`: `\|` and `?` added to `ONE_CHAR_OPERATORS` (both lexed as plain `operator` tokens; `?` and `:` remain two separate tokens, confirmed by a dedicated unit test; no other tokenizer rule changed). SPEC §6.4.1's operator list and F2 (§2) updated to match — F2 now says the ternary conditional expression exists on current AL runtimes (confirmed live: `cond ? a : b` in `TemplateValues.Codeunit.al:322`) but v1 still mutates at statement level regardless, since F1 (no short-circuit guarantee) and F3 (eager argument evaluation) are the binding reasons, independent of whether a ternary exists. Fixed in `generator/src/generate.ts`: the per-file candidate-generation loop (`tokenize` → `findObjectHeader`/`findProcedures`/`findConditions`/`findSimpleStatements` → operator `apply`) is now wrapped in a try/catch; any error there is recorded as a `skipped.json` entry `{file, line: 0, reason: "tokenize-error: <message>"}` and the run continues with the remaining files (§6.4.6, §6.4.9), instead of the previous unguarded direct `tokenize()` call that crashed the whole process; the later per-file `aut-schemata`-writing loop still aborts on error (a file only reaches it after its candidates were already generated successfully) but now re-throws with the relative file path prefixed, so a genuine failure there is at least attributable. TDD: `generator/test/tokenizer.test.ts` gained two hand-written unit tests (a `filter("A" \| "B")` line and a `b ? '1' : '2'` line, asserting exact token kind/text sequences) plus a new hand-written golden fixture pair `fixtures/tokenizer/filter-and-ternary.{al,tokens.json}`; `generator/test/generate-robustness.test.ts` is new and asserts `generate()` does not throw when one of two input files contains a stray `~` (an invalid AL character), that the other file's mutants are still produced, and that `skipped.json` names the bad file with a `tokenize-error:` reason — all written and run red before the fix, then green after. `cd generator && npm test`: 109/109 pass (105 pre-existing + 4 new); the five `fixtures/generator/*` golden cases are untouched and still pass byte-for-byte; `orchestrator/tests` (untouched by this task): 212/212 pass. Real-input acceptance, run against the live `out/aut-original` copy (1,072 `.al` files, BC 29, read-only): a throwaway scan confirmed **0** tokenize failures across all 1,072 files (down from 20); the exact command from the T28 row (`node generator/dist/src/cli.js generate --aut out/aut-original --out out/gen-t21b --core-app-id 6f1d2c3a-8b4e-4d5f-9a6b-7c8d9e0f1a2b --core-app-version 1.0.0.0 --max-mutants 0 --seed 1 --only-objects 72918635 --operators REL,BOOL,NOT,COND,DEL,INSFLAG`) now completes and writes `out/gen-t21b/mutants.json` (exact mutant/skipped counts in `task-T21b-report.md`). | 2026-09-17 |
| **T28 second attempt: with T21b's tokenizer fix in place, `generate` now succeeds against the real AUT (157 mutants for codeunit 72918635, 0 tokenize failures, matching the verified smoke run) — but `Build-MutSchemata`'s compile of the generated `aut-schemata` app now fails on a DIFFERENT, previously-unseen blocker: CodeCop naming rule AA0072 firing on the schemata generator's own injected scaffolding, not on anything mutant-specific.** `Invoke-MutationRun.ps1 -ConfigPath mutation.config.json -RunNo 3` reused `out/runs/3`'s existing `initialize`/`environment`/`baseline`/`references`/`coverage` markers, then `generate` wrote `out/runs/3/gen/{mutants.json (157 mutants), linemap.json, skipped.json}` cleanly, then `Compile-MutApp` on `out/runs/3/gen/aut-schemata` returned diagnostic `AA0072 at 'Authentication\Codeunit\AuthShareDetection.Codeunit.al:66': The name of MutationCore is not valid. The name of variables and parameters must be suffixed with the type or object name.` — this is the AUT's own `.cli-ruleset-localdeploy.json` (the same ruleset `mutation.config.json`'s `rulesets.file` points at for compiling the real AUT/test app) firing on the schemata generator's own injected local `MutationCore: Codeunit "MUT Mut";` (the handle every injected `case true of MutationCore.Active(<id>): ...` guard block calls), because `Schemata.psm1`'s `Build-MutSchemata` (its `Compile-MutApp` call, ~lines 289-291/320-321) reuses `$Config.rulesets.file` verbatim to compile the synthetic schemata app instead of a ruleset of its own. `Resolve-MutCompileErrorMutantIds` (Schemata.psm1) correctly found that diagnostic line 66 (the `var` section, before any guard block) falls inside no mutant's `[startLine, endLine]` range in `linemap.json`, so per §6.5.4 step 4 it correctly refused to treat this as an excludable-mutant compile error and threw immediately rather than spinning its 10-iteration retry loop uselessly — the abort fired exactly as designed and was not weakened. Root cause: the schemata boilerplate (`MutationCore`, and each guard block's `MutCond_n: Boolean;` locals) was previously only ever compiled against the small fixture app's (lenient) ruleset; this is the first time it has been compiled under the real AUT's strict custom CodeCop rules, and the boilerplate's naming doesn't conform. Latent, not yet observed: the same strict ruleset likely also has an unused-variable/unused-local rule (e.g. AA0137-shaped) that would fire if a later compile-error-exclusion round ever drops every guard arm referencing one `MutCond_n` local while leaving its declaration behind — untested here since the pipeline never got past this first AA0072 failure to reach a retry round. Fix needed (not attempted under this task; the generator's guard template and mutant output are unchanged and out of scope here): compile the generated schemata app with its own dedicated ruleset (downgrading/suppressing naming and unused-variable style rules for this synthetic scaffolding) instead of reusing the AUT's real ruleset file verbatim. Environment left idle and unchanged: `mut-spike-02`'s `mutationSetup(0)` shows `activeMutantId: 0, currentRunNo: 0`; `Continia Banking` (AUT) 29.0.0.0, `Continia Banking - Base App - Test Suite` 29.0.0.0, and `Mutation Core` 1.0.0.0 all still installed (no redeploy); no orchestrator or CLI process left running; `out/runs/3/gen/` (this attempt's `mutants.json`/`linemap.json`/`skipped.json`/`aut-schemata/`) and the pre-existing `initialize`/`environment`/`baseline`/`references`/`coverage` artifacts left in place for a resumed attempt once the schemata ruleset gap is fixed. | 2026-09-17 |
| **T28 fourth attempt: commit 61ebd20's fix for the AA0072 blocker above introduced a NEW blocker — the generated schemata ruleset file itself is invalid JSON per the AL compiler's own ruleset schema, so `Build-MutSchemata`'s compile of `aut-schemata` still fails, now on a different diagnostic that maps to no mutant at all.** `Invoke-MutationRun.ps1 -ConfigPath mutation.config.json -RunNo 3` (launched as a true background process this time, `nohup ... &` into `out/runs/3/run.log`/`run.err.log`, confirmed alive via `Get-CimInstance Win32_Process` immediately after launch and exited on its own well within the first bounded poll) reused every pre-existing marker/artifact (`initialize`/`environment`/`baseline`/`references`/`coverage`/`gen/mutants.json` — still 157 mutants, 0 tokenize failures) and reached `Build-MutSchemata` again, which now generates `out/rulesets/.cli-ruleset-schemata.json` (per 61ebd20's new `New-MutSchemataRulesetJson`/`Write-MutSchemataRuleset` in `Schemata.psm1`) and points the schemata compile at it instead of the AUT's own ruleset — but the compile immediately failed with `AL1033 at no File/Line: An error occurred while loading the included rule set file C:\GeneralDev\al\al-mutation\out\rulesets\.cli-ruleset-schemata.json - Required property 'name' not found in JSON. Path '', line 20, position 1.` `New-MutSchemataRulesetJson` (Schemata.psm1 ~line 166) builds `[pscustomobject]@{ includedRuleSets = ...; rules = ... }` with no `name` property at all; every other ruleset file in this repo (e.g. the AUT's own `.cli-ruleset-localdeploy.json`, confirmed by direct comparison) has a top-level `"name"` string, which the AL compiler's ruleset loader (`AL1033`) requires — this generated file is missing it, so the compiler cannot load the ruleset at all, and the schemata compile fails before a single one of the actual per-file diagnostics (AA0072/AA0137, the two rules 61ebd20 meant to downgrade) is ever evaluated. `Resolve-MutCompileErrorMutantIds` correctly found this diagnostic has no `File`/`Line` at all (a ruleset-load-time failure, not a per-file compile diagnostic), so per §6.5.4 step 4 it correctly refused to treat it as excludable and threw immediately rather than spinning its retry loop uselessly — the abort fired exactly as designed and was not weakened, and the ruleset/schemata were not hand-edited to work around it, per this task's explicit guardrail. AA0072/AA0137's actual downgrade values in the generated ruleset were never exercised because the file never loaded; whether they would have been sufficient once `name` is added is still unverified. Environment left idle and unchanged: `mut-spike-02`'s `mutationSetup(0)` shows `activeMutantId: 0, currentRunNo: 0`; no orchestrator or CLI process left running (`Get-CimInstance Win32_Process` clean); `git -C "Continia Banking" status --short` unchanged (11 lines, same as before this attempt) — the read-only AUT source tree was never touched. `out/runs/3/*` (including `gen/`) left in place for a resumed attempt once `New-MutSchemataRulesetJson` is fixed to emit a `name` (and ideally a `description`) property. | 2026-09-17 |
| **T28 fifth attempt: commit 0d3aae7's fix for the AL1033 blocker above (adding `name` to the generated schemata ruleset) lets the ruleset load and its AA0072/AA0137 downgrades take effect, but `Build-MutSchemata`'s compile of `aut-schemata` still fails, now on a THIRD CodeCop rule (AA0021, var-declaration type ordering) the generated ruleset doesn't downgrade.** `Invoke-MutationRun.ps1 -ConfigPath mutation.config.json -RunNo 3` (launched via `nohup ... &`, confirmed alive via `Get-CimInstance Win32_Process` and exited on its own within the first bounded `Monitor` poll) reused every pre-existing marker/artifact (`initialize`/`environment`/`baseline`/`references`/`coverage`/`gen/mutants.json` — still 157 mutants, 0 tokenize failures) and reached `Build-MutSchemata` again; the regenerated `out/rulesets/.cli-ruleset-schemata.json` now has a top-level `"name"` and loaded successfully (confirmed by reading it directly: `{"name": "Mutation schemata ruleset", "includedRuleSets": [...], "rules": [AA0072→Info, AA0137→Info]}`), and the compile got past line 66's AA0072 diagnostic from the second attempt — but then failed with `AA0021 at 'Authentication\Codeunit\AuthShareDetection.Codeunit.al:295': Variable declarations should be ordered by type. Variables should be sorted like this: Record, Report, Codeunit, XmlPort, Page, Query, Notification, BigText, DateFormula, RecordId, RecordRef, FieldRef, and FilterPageBuilder. The rest of the variables are not sorted.` Line 295 is the schemata generator's own injected `MutationCore: Codeunit "MUT Mut";` local inside `local procedure AppendAccountForBank(...)`'s `var` section, appended AFTER the procedure's own pre-existing `Accounts: List of [Code[20]];` local — a `Codeunit`-typed var declared after a "rest"-group var, which AA0021 forbids; the schemata generator appends `MutationCore`'s declaration at the end of each procedure's existing `var` block rather than inserting it in type order (or first), so this only failed to surface earlier because it happens to be the first procedure (in compile order) whose pre-existing locals include something in the unordered "rest" group before where `MutationCore` gets appended. `Resolve-MutCompileErrorMutantIds` correctly found line 295 (inside the `var` section, before any guard block) maps to no mutant's `[startLine, endLine]` range in `linemap.json`, so per §6.5.4 step 4 it correctly refused to treat this as excludable and threw immediately rather than spinning its 10-iteration retry loop uselessly — the abort fired exactly as designed and was not weakened; per this task's explicit guardrail, the ruleset and the generator's injection logic were not hand-edited to work around it. Fix needed (not attempted; out of scope for this task): either downgrade `AA0021` to `Info` in the same generated schemata ruleset (simplest, consistent with the ruleset's stated purpose — the schemata app is generated, never shipped, never read by humans), or change the schemata generator to insert `MutationCore`'s declaration in correct type order (or first) instead of appending it. Unresolved: whether AA0021 is the last var-ordering-shaped rule waiting in this ruleset — a full compile of all 157 mutants' worth of injected declarations under the real AUT's strict ruleset has still never completed, so a fourth rule could still be hiding behind this one. Environment left idle and unchanged: `mut-spike-02`'s `mutationSetup` shows `activeMutantId: 0, currentRunNo: 0` (checked both before launch and after stand-down); no orchestrator or CLI process left running; `git -C "Continia Banking" status --short` unchanged (11 lines, same as every prior attempt) — the read-only AUT source tree was never touched. `out/runs/3/*` (including `gen/` and the regenerated `out/rulesets/.cli-ruleset-schemata.json`) left in place for a resumed attempt once the AA0021 gap is fixed; `out/runs/3` was not deleted. | 2026-09-17 |
| **T25c RESOLVED the whole rule-by-rule whack-a-mole above (AA0072 → AL1033 → AA0021, four blockers in five attempts): `Build-MutSchemata` now writes `gen/aut-schemata/.vscode/settings.json` containing exactly `{ "al.codeAnalyzers": [] }` on every compile-loop iteration (new `Write-MutSchemataAnalyzerSettings` in `Schemata.psm1`), so no style analyzer runs against the generated schemata app at all — only genuine compiler errors can fail the compile.** The generated sibling ruleset (`.cli-ruleset-schemata.json`, downgrading `AA0072`/`AA0137` to `Info`) is kept and still passed via `-Ruleset` as a harmless second line of defence, but no longer does the actual suppressing. Verified against the REAL app, not just unit tests: generated 157 mutants for codeunit 72918635 into a scratch dir (`out/t25c/gen`, `--aut out/aut-original`, same flags as the pipeline), wrote the empty-analyzer settings file via the real `Write-MutSchemataAnalyzerSettings` code path, then compiled `out/t25c/gen/aut-schemata` (all files under the real 1,072-file AUT tree, not a fixture) with `.tools/continia.exe compile ... --env 65eb4296-df3c-4ceb-a189-c3d029d701d0` (`mut-spike-02`, compile only, no deploy) — twice, for confirmation. Both runs: `success: true`, `diagnosticCounts: { error: 0, warning: 31, info: 42 }`, `analyzerLoadFailures: 0`, an `.app` file produced. Compile-only wall time was seconds once symbols were cached (~11s; the first run, which also refreshed symbols from the environment, was comparably fast) — well inside the "roughly a minute or two" full-app estimate. Orchestrator suite: 213/213 green (one new test added for the settings-file write; all 212 pre-existing tests still pass). `out/t25c` is git-ignored scratch, left in place. | 2026-09-17 |
